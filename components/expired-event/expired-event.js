import classes from "./expired-event.module.css";

function ExpiredEvent() {
  return (
    <div className={classes.expired_event}>
      <h1>Event not available</h1>
    </div>
  );
}

export default ExpiredEvent;
